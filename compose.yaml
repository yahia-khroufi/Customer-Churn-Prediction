services:
  churn-app:
    build: .
    image: customer-churn:local
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      MODEL_PATH: /app/models/churn_pipeline.joblib
    restart: unless-stopped
